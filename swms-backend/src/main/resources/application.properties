# ============================================================
# DATA SOURCE — MySQL connection
# ============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/swms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=sumit@211b320
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================================================
# JPA / HIBERNATE
# ddl-auto=update  →  Hibernate creates / alters tables on startup.
# For production use 'validate' or Flyway migrations.
# ============================================================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================================
# JWT CONFIGURATION
# Secret must be >= 256 bits (32 bytes) for HS256.
# Expiration = 24 hours in milliseconds.
# ============================================================
app.jwt.secret=SWMS_SUPER_SECRET_KEY_FOR_JWT_AUTH_MUST_BE_256_BITS_MIN
app.jwt.expiration-ms=86400000

# ============================================================
# SERVER
# ============================================================
server.port=8080

# ============================================================
# LOGGING
# ============================================================
logging.level.com.swms=DEBUG
logging.level.org.springframework.security=DEBUG
